import { MobileHomepageAdminClient } from "@/components/admin/MobileHomepageAdminClient";

export const metadata = {
  title: "Mobile Homepage — Admin",
};

export default function AdminMobileHomepagePage() {
  return <MobileHomepageAdminClient />;
}
