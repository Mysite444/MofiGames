import { BrowsePageSkeleton } from "@/components/skeletons/BrowsePageSkeleton";

export default function Loading() {
  return <BrowsePageSkeleton desktopColumns="6" bannerCount={0} gridCount={24} />;
}
